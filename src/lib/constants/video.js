const PLATFORMS = Object.freeze({
	youtube: 'YouTube',
	twitch: 'Twitch',
});

export const VIDEO_CONSTANTS = {
	PLATFORMS,
};

export const VIDEO_MIN_WIDTH = 192;
export const VIDEO_MIN_HEIGHT = 108;

export const VIDEO_MAX_HEIGHT = 1000;
